name: Issue 54 repair marker

on:
  workflow_dispatch:

# Connector-triggered synchronization marker for the temporary repair CI.
jobs:
  marker:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo marker
