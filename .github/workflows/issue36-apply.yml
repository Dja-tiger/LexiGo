name: Apply Issue 36 React UI

on:
  push:
    branches: [fix/issue-36-react-owned-ui]

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: fix/issue-36-react-owned-ui
          fetch-depth: 0
      - name: Apply verified codemod
        run: python tools/issue36_react_ui_patch.py
      - name: Remove one-shot codemod
        run: rm tools/issue36_react_ui_patch.py
      - name: Commit React-owned UI patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add frontend/components/lexigo-premium-app.tsx tools/issue36_react_ui_patch.py
          git commit -m "refactor(ui): render enhancements declaratively"
          git push origin HEAD:fix/issue-36-react-owned-ui
