name: lexigo-production

services:
  postgres:
    image: postgres:18.4-alpine
    env_file: ../env/prod.env
    volumes: [prod_postgres:/var/lib/postgresql]
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 10

  redis:
    image: redis:8.0-alpine
    command: ["sh", "-c", "exec redis-server --appendonly yes --requirepass \"$${REDIS_PASSWORD}\""]
    env_file: ../env/prod.env
    volumes: [prod_redis:/data]
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a \"$$REDIS_PASSWORD\" ping 2>/dev/null | grep -q PONG"]
      interval: 10s
      timeout: 5s
      retries: 10

  api:
    image: ghcr.io/dja-tiger/lexigo-api:${IMAGE_TAG:?IMAGE_TAG is required}
    env_file: ../env/prod.env
    depends_on:
      postgres: { condition: service_healthy }
      redis: { condition: service_healthy }
    restart: always
    healthcheck:
      test: ["CMD", "/app/lexigo", "healthcheck"]
      interval: 10s
      timeout: 3s
      retries: 10
    deploy:
      resources:
        limits: { cpus: "1.0", memory: 512M }

  web:
    image: ghcr.io/dja-tiger/lexigo-web:${IMAGE_TAG:?IMAGE_TAG is required}
    env_file: ../env/prod.env
    depends_on:
      api: { condition: service_healthy }
    restart: always
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 10
    deploy:
      resources:
        limits: { cpus: "0.5", memory: 384M }

  caddy:
    image: ghcr.io/dja-tiger/lexigo-caddy:2.11.4-cloudflare-v0.2.4
    env_file: ../env/prod.caddy.env
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ../Caddyfile:/etc/caddy/Caddyfile:ro
      - prod_caddy_data:/data
      - prod_caddy_config:/config
    depends_on:
      api: { condition: service_healthy }
      web: { condition: service_healthy }
    restart: always
    healthcheck:
      test: ["CMD", "caddy", "validate", "--config", "/etc/caddy/Caddyfile", "--adapter", "caddyfile"]
      interval: 30s
      timeout: 10s
      retries: 5

volumes:
  prod_postgres:
  prod_redis:
  prod_caddy_data:
  prod_caddy_config:
