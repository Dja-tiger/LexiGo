# Current Task

## Identity

- Issue: dependency maintenance; supersedes stale Dependabot PR #478 on fresh `main`.
- Branch: `chore/go-redis-9-22-fresh-main`
- Base SHA: `e034fa155b18f1053f43ac492417ac4955169cf7`
- Head SHA: resolve from live branch ref
- PR: create after implementation read-back.

## Objective

Upgrade `github.com/redis/go-redis/v9` from `v9.21.0` to `v9.22.0` on the current repository base and prove compatibility through the repository-owned backend/security/integration/container gates.

## Scope

Apply only the dependency graph changes generated by Dependabot PR #478 to the current `main`; validate the release-specific compatibility risks before merge.

## Non-goals

No Redis feature adoption, no client-side caching, no automatic pipelining, no timeout/retry behavior redesign, no application/runtime logic changes, no frontend/Figma changes, no production deployment configuration changes.

## Allowed paths

`.agents/current/*`, `backend/go.mod`, `backend/go.sum`.

## Prohibited paths

All other application, workflow, deployment, migration, API, frontend and documentation paths.

## Runtime owners

`backend/internal/platform/redis/redis.go` owns production Redis client construction. Existing call sites in health, rate limiting and server wiring consume that client without opting into new 9.22 experimental features.

## Documentation owners

`.agents/current/*` records task scope and immutable-head evidence. Durable project state is reconciled separately after merge if needed.

## Invariants

- Existing explicit `DialTimeout=5s`, `ReadTimeout=2s`, `WriteTimeout=2s`, `PoolSize=20` and `MinIdleConns=2` remain unchanged.
- `WaitAOF` is not used by the repository.
- No experimental 9.22 feature is enabled.
- Dependency-only runtime behavior must remain compatible with PostgreSQL/Redis integration and rate-limit/session flows.

## Acceptance criteria

- `go.mod`/`go.sum` contain the exact 9.22.0 dependency graph generated by Dependabot.
- No code changes are required for the `WaitAOF` signature correction.
- Existing explicit I/O timeouts remain authoritative despite upstream default changes.
- Backend formatting/static/unit/race/security/integration and container gates pass on one immutable final head.
- PR reviews/threads are clean; merge uses expected head SHA.

## Required checks

Repository CI selected for backend dependency changes, including `govulncheck`, PostgreSQL/Redis integration and API container build.

## Risks

Upstream 9.22 changes retry/backoff and keep-alive defaults that LexiGo does not currently override. Full backend/integration validation is therefore required even though explicit dial/read/write timeouts are stable.

## Rollback

Revert the dependency upgrade squash merge to restore go-redis `v9.21.0`.
