# Current Task

No mergeable atomic production slice is active on the current `main`.

PR #355 completed the proof-only Home hero-decoration CSS inventory and is deployed as `f279bc577704f3f68bf587f4c88b474a62929c02`.

Draft PR #354 remains open from obsolete base `df3cd097cbd159a4d441aea4ce783043dabe36ec`. It must remain Draft until this reconciliation merges, after which its bounded source-contract change must be reconstructed onto the exact new `main` and fully revalidated.

Do not begin another deletion slice before PR #354 is either safely delivered through exact-SHA stage validation or explicitly closed with recorded evidence.
